services:
  - type: web
    name: app
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free 
    autoDetect: true 
    envVars:
    - key: NODE_ENV
      value: production